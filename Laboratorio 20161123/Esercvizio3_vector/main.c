#include "vector.h"

int main(void)
{
	struct vector vettore;
	vector_create(&vettore, vettore.size);
	//vector_destroy(&vettore);
	//struct vector *vett = new_vector(5);
	//delete_vector(vett);
	return 0;
}
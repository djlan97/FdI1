#define _USE_MATH_DEFINES
#include <math.h>
extern double coseno(double x);
extern double fatt(int n);
extern double pot(double b, int e);
extern double coseno2(double x);
int main(void){

	double x=pot(0,0);

	double cos = coseno2(M_PI);


	return 0;
}